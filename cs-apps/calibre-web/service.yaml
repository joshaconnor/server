apiVersion: v1
kind: Service
metadata:
  labels:
    app: cs-calibre-web
  name: cs-calibre-web
spec:
  ports:
    - name: "8083"
      port: 8083
      targetPort: 8083
  selector:
    app: cs-calibre-web

